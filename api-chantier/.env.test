# Isolated test DB — used only by npm test / migrate:test (Phase 15).
# Does NOT point at the demo Docker volume (port 5432 / database chantier).
DATABASE_URL=postgres://chantier:chantier@127.0.0.1:5433/chantier_test
JWT_SECRET=test-only-jwt-secret-not-for-demo
JWT_EXPIRES_IN=1h
NODE_ENV=test
CORS_ORIGIN=*
