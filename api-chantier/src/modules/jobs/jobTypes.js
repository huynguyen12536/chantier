/** Imp-10 — allowed job type constants (allow-list). */
export const JOB_PLATFORM_NOOP = 'jobs.platform_noop';

/** Imp-10 Wave B1 JB-01 — redispatch Imp-09 catalog event (delivery only). */
export const JOB_REALTIME_REDISPATCH_CATALOG = 'jobs.realtime.redispatch_catalog';

/** Outbound email delivery (worker only — never from FE). */
export const JOB_MAIL_SEND = 'mail.send';
