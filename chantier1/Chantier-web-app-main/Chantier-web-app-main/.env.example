# Copy to .env for local dev (optional — app.config.js already sets Supabase for Bolt)
EXPO_PUBLIC_SUPABASE_URL=https://afgveikzneaablcuzwdb.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
EXPO_FORCE_WEBCONTAINER_ENV=1

# Password reset OTP emails — set as Supabase Edge Function secrets (NOT in client bundle):
# Project Settings → Edge Functions → Secrets (or supabase secrets set)
#
#   MAIL_HOST=live.smtp.mailtrap.io
#   MAIL_PORT=587
#   MAIL_USERNAME=api
#   MAIL_PASSWORD=your_mailtrap_domain_api_token
#   MAIL_FROM_NAME=ATN ChanTier
#   MAIL_FROM_EMAIL=noreply@smtp02.dfm-europe.com
#
# Deploy edge functions after migration (use npx if supabase CLI is not installed globally):
#   npx supabase login
#   npx supabase link --project-ref afgveikzneaablcuzwdb
#   npx supabase db push
#   npx supabase functions deploy send-password-reset-otp
#   npx supabase functions deploy verify-password-reset-otp
#   npx supabase functions deploy reset-password-with-otp
