# OBSOLETE — Legacy Supabase Edge Functions (Deno)

**Status:** DEPRECATED / ARCHIVED (Phase 15 — DEF-P15-004)  
**Do not deploy.** Local product uses Unified API compat mounts:

- `POST /functions/v1/create-user`
- `POST /functions/v1/delete-user`

These Deno sources are retained for historical reference only.
