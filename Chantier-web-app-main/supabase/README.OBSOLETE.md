# OBSOLETE MARKER

Directory `supabase/` may retain migrations / CLI temp files.  
Edge Function **source** was moved to `archive/legacy-supabase-edge-functions/`.

Active runtime uses the Unified Backend compatibility layer, not Deno Edge.
