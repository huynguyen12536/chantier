/** Visible tab bar content height (excluding safe-area inset). */
export const TAB_BAR_HEIGHT = 70;

/** Padding below the status bar / notch for header titles and controls. */
export const HEADER_CONTENT_PADDING = 12;

/** Max content width on web / tablet (aligned with other admin screens). */
export const CONTENT_MAX_WIDTH = 960;
